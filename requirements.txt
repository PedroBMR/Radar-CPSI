# Runtime dependencies for Radar CPSI
httpx>=0.27,<1.0        # HTTP client with timeout/retry support
PyYAML>=6.0,<7.0        # Config / keyword file parsing
python-dotenv>=1.0,<2.0 # Load .env for local runs (no-op in CI)

# Dev / test dependencies
pytest>=8.0,<9.0
